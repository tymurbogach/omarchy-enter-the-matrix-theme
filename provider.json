{
  "//": [
    "The provider. Everything else in the pack is machinery and reads its names",
    "from here: the CLI, both derivers, the hooks, install.sh and uninstall.sh.",
    "",
    "The four pieces -- wallpaper, screensaver, lock, boot -- are not really",
    "matrix-specific. A provider hands over a shader, its assets and the words on",
    "the boot splash; the machinery around it stays generic. Keeping that seam",
    "visible is the point of this file: a second provider should not need a",
    "single line of bash changed.",
    "",
    "Installed to ~/.local/share/omarchy-matrix/provider.json so the CLI can",
    "still read it while another theme is current -- `suspend` and `lock off`",
    "run exactly then, and ~/.local/state/omarchy/current/theme points elsewhere."
  ],

  "slug": "enter-the-matrix",
  "displayName": "Enter the Matrix",
  "cli": "omarchy-matrix",
  "//ipc": "The IpcHandler target the service registers; `omarchy-shell <ipc> ...`.",
  "ipc": "matrix",
  "accent": "#7bae4e",
  "repoUrl": "https://github.com/tymurbogach/omarchy-enter-the-matrix-theme",

  "plugin": {
    "id": "io.github.tymurbogach.enter-the-matrix",
    "dir": ".",
    "files": [
      "manifest.json",
      "Service.qml",
      "MatrixRain.qml",
      "matrix.frag.qsb",
      "glyphs.png"
    ]
  },

  "widget": {
    "id": "io.github.tymurbogach.enter-the-matrix.widget",
    "//repo": [
      "The widget lives in its own repo, because a plugin repo holds one",
      "manifest at its root. install.sh fetches the commit that \"ref\" names",
      "(a full SHA) and caches it. A cache at that commit needs no network.",
      "MATRIX_WIDGET_SRC overrides this for local development against an",
      "uncommitted checkout. Why a fetch and not a git submodule: see the trap",
      "headed \"The widget comes from its own repo\" under \"Traps already paid",
      "for\" in CONTRIBUTING.md."
    ],
    "repo": "https://github.com/tymurbogach/omarchy-matrix-widget",
    "ref": "1684e3efe323b3541870f801d0c831b7bb6ee5b9",
    "files": ["manifest.json", "Panel.qml"],
    "section": "right"
  },

  "//rainFiles": [
    "What the lock clone needs beside Omarchy's own sources: the shader, its",
    "atlas and the QML that draws them. derive-lock.py copies these into the",
    "clone before it patches LockView.qml."
  ],
  "rainFiles": ["MatrixRain.qml", "matrix.frag.qsb", "glyphs.png"],

  "//liveBackground": [
    "The background that turns the desktop rain on. The plugin watches for the",
    "-live- in its name. It is not in backgrounds/, so the theme alone never",
    "offers it. The pack links it into Omarchy's folder for your own",
    "backgrounds of this theme, ~/.config/omarchy/backgrounds/<slug>/.",
    "Omarchy lists that folder first, so `omarchy theme set` starts on the rain."
  ],
  "liveBackground": "live/0-live-rain.png",

  "plymouth": {
    "theme": "omarchy-matrix",
    "name": "Omarchy Matrix",
    "description": "Omarchy's splash with the line from the start of the film.",
    "//lines": "The four lines from the start of the film, in order.",
    "lines": [
      "Wake up, Neo...",
      "The Matrix has you...",
      "Follow the white rabbit.",
      "Knock, knock, Neo."
    ],
    "//modes": [
      "What is typed on the way OUT, keyed by the mode plymouthd was started",
      "with -- which is what the theme's `Plymouth.GetMode()` reports. `boot` is",
      "`lines` above; anything named here replaces it for that mode only.",
      "",
      "THERE ARE ONLY TWO EXITS, not three. `plymouth-halt.service`,",
      "`plymouth-poweroff.service` and `plymouth-kexec.service` all start the",
      "daemon with --mode=shutdown -- only `plymouth-reboot.service` differs. So",
      "a halt cannot be told apart from a power off from inside the script, and",
      "a `halt` key here would be config that never runs.",
      "",
      "The exits are SHORT: the splash lives until the machine does, which is a",
      "couple of seconds. Only the first line is reliably seen, so the first line",
      "is the payoff -- see EXIT_PACE in lib/derive-plymouth.py, which types",
      "these faster and holds them for less than the boot's.",
      "",
      "Shutdown answers the boot: it opens by waking Mr. Anderson and closes by",
      "saying goodbye to him, and `Unplug him.` is what is actually happening.",
      "Reboot is the film's own definition of one -- a deja vu is a glitch in the",
      "Matrix, and it happens when they change something."
    ],
    "modes": {
      "shutdown": [
        "Goodbye, Mr. Anderson.",
        "Unplug him."
      ],
      "reboot": [
        "Déjà vu.",
        "They changed something."
      ]
    },
    "//color": [
      "The typed lines' colour, r, g, b in 0..1 -- and DELIBERATELY ABSENT.",
      "",
      "Without it the lines take the theme's own `green` from colors.toml, so",
      "there is one green in the pack rather than two that drift apart.",
      "",
      "NOT the shader's #00FF41: at the size these lines are drawn, on black,",
      "that reads as glare rather than as a monitor. Set this only to break the",
      "tie with the palette on purpose."
    ],
    "//dialogColor": [
      "The passphrase box, a different colour from the lines on purpose: in the",
      "film the terminal runs green and the boxed prompt is an ice blue.",
      "",
      "MEASURED, not chosen -- twice. #75C8D9 was the first pass, sampled off a",
      "wider shot of the frame; on the reference's own close-up screenshot the",
      "flat fill of the band itself (not the bloom on its brightest pixels, not",
      "the border) averages a good deal whiter, #B2DDE5. That is what is here",
      "now. What matters is that BLUE still sits above green (E 229 > D 221 >",
      "B 178 in 0..255); the first guess at any of this was a mint #7FFFE0,",
      "green-dominant, and it read as the wrong colour immediately beside the",
      "real thing.",
      "",
      "Omit it and the box takes `color`, so an older provider is unaffected."
    ],
    "dialogColor": [0.698, 0.867, 0.898],
    "//titles": [
      "In the panel's filled title band, the way an old window manager",
      "captioned a window. The panel wears one of FOUR of these at a time --",
      "these two, and the grantedText/deniedText below -- and swaps the whole",
      "baked band rather than composing a frame and a caption at boot."
    ],
    "boxTitle": "enter password",
    "progressTitle": "RTF CONTROL",

    "//grantedDenied": [
      "The one-shot feedback, worn by the title band itself -- not layered",
      "over the content row underneath it. Both were tried in that row first,",
      "under a band that still said `enter password` or the progress title, and",
      "that is two captions saying the same thing in two places at once.",
      "",
      "ACCESS GRANTED plays once, the moment the passphrase is accepted, then",
      "the band hands over to progressTitle and the real progress track.",
      "",
      "ACCESS DENIED plays when Plymouth re-asks for a passphrase, then the band",
      "hands back to boxTitle. That is a HEURISTIC, not a real signal: the",
      "script API never tells a rejected passphrase apart from the user",
      "backspacing the field to nothing by hand, because both look exactly like",
      "a fresh display_password_callback with bullets back at 0. See",
      "derive-plymouth.py."
    ],
    "grantedText": "ACCESS GRANTED",
    "deniedText": "ACCESS DENIED"
  }
}
